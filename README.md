# Private Earth — local dev

Run locally:

```bash
# install deps
npm install
# run dev server
npm run dev
```

This repository hosts the Private Earth marketing site and project pages. See `app/projects` for project content and `public/images` for logo and project visuals.
